diff --git a/src/main.rs b/src/main.rs
index e41936c..a19229f 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -4,7 +4,7 @@ mod modules;
 use imgui::*;
 use modules::input::MouseInput;
 use std::collections::HashMap;
-use std::time::{ Duration, Instant };
+use std::time::Duration;
 use modules::ui::support;
 use modules::config::{ Setup, SettingsIO, WEAPON_CLASSES };
 use modules::core::{
@@ -115,9 +115,6 @@ fn main() {
 
     log_debug("Completed setup initialization");
 
-    // --- Dynamic Frame Cap State ---
-    let last_activity = Instant::now();
-
     let mut settings_io = SettingsIO::new();
 
     let gfck_path = std::path::PathBuf::from("lib/GFCK.dll");
@@ -272,12 +269,8 @@ fn main() {
                 let _ = ghost_manager.find_and_set_window_handle("Impusle Config");
             }
 
-            // Dynamic frame cap
-            let idle = last_activity.elapsed() > Duration::from_secs(15);
-
-            let current_frame_cap = if rcs_enabled && !idle { 100 } else { 30 };
-            let frame_time = Duration::from_secs_f32(1.0 / (current_frame_cap as f32));
-            std::thread::sleep(frame_time);
+            // Fixed UI frame cap — the heavy work happens in the control thread.
+            std::thread::sleep(Duration::from_secs_f32(1.0 / 60.0));
 
             let window_flags =
                 WindowFlags::NO_RESIZE |
diff --git a/src/modules/core/mod.rs b/src/modules/core/mod.rs
index e0b71be..44932f2 100644
--- a/src/modules/core/mod.rs
+++ b/src/modules/core/mod.rs
@@ -1,5 +1,4 @@
 pub mod control;
-pub mod xmod_state;
 pub mod hotkey_handler;
 pub mod process_ghost;
 pub mod logger;
diff --git a/src/modules/core/xmod_state.rs b/src/modules/core/xmod_state.rs
deleted file mode 100644
index ab903f7..0000000
--- a/src/modules/core/xmod_state.rs
+++ /dev/null
@@ -1,4 +0,0 @@
-pub struct XmodState {
-    pub x_flip: i32,
-    pub x_once_done: bool,
-}
\ No newline at end of file
diff --git a/src/modules/input/mod.rs b/src/modules/input/mod.rs
index e80265d..3c707b8 100644
--- a/src/modules/input/mod.rs
+++ b/src/modules/input/mod.rs
@@ -1,6 +1,5 @@
 pub mod gfck;
 pub mod ghub;
 pub mod mouse_input;
-pub mod mouse_command;
 
 pub use mouse_input::MouseInput;
diff --git a/src/modules/input/mouse_command.rs b/src/modules/input/mouse_command.rs
deleted file mode 100644
index 6576762..0000000
--- a/src/modules/input/mouse_command.rs
+++ /dev/null
@@ -1,7 +0,0 @@
-#![allow(dead_code)]
-pub enum MouseCommand {
-    Move(i32, i32),
-    Click(i32),
-    Down(i32),
-    Up(i32),
-}
